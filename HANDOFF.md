# ThermoVerse 專案交接

更新日期：2026-09-11 22:30（臺灣時間）

## 下個 Session 先做什麼

1. **GitHub Pages 線上預覽啟用（若需對外展示）**：
   - 目前倉庫為 Private：若需使用免費用 GitHub Pages 產生公開網址，請在 GitHub 倉庫 **Settings** > **Danger Zone** 改為 **Public**，隨後在 **Settings** > **Pages** 的 Branch 選擇 **`gh-pages`** 分支，即可獲得線上官方網址（`https://thermo2026.github.io/thermoverse-website/`）。
2. **Google 文件條款確認與修改**：
   - 開啟並重新讀取 Google 文件的 `Statement of Work` 與 `Website Guidelines` 分頁。
   - 確認 Google 文件是否已套用本檔「Google 文件待修改項目」列出的內容。
   - 修改後重新讀取相關段落，確認文字、日期與條款均已正確寫入。
3. **交付節點控管**：
   - 第一版：2026-09-12
   - 第二版：2026-09-14
   - 最終版：2026-09-16

## Google 文件

- 文件名稱：`FUTEX SOW - FINAL DRAFT (9/8)`
- 網址：<https://docs.google.com/document/d/1cmcI2cRC-Khs8BPHuHmrrvdpkz18BtXx3rQ4l-voZJE/edit?tab=t.ms0kvlrbrygx#heading=h.7itvbnclhdq4>
- 分頁：
  - `Project Overview`：`t.0`
  - `Statement of Work`：`t.ms0kvlrbrygx`
  - `Website Guidelines`：`t.320dwjls9zvy`
- 使用者已表示 Google 文件編輯權限已開啟。
- 上個 Session 的 Google Drive 編輯工具暫時未出現在可用工具清單，因此尚未確認遠端文件已成功修改。
- 不要使用瀏覽器模擬輸入修改 Google Docs；優先使用 Google Drive／Google Docs 連接器，修改後必須重新讀取驗證。

## 已確認的專案決策

- 網站為一次性中英文優化專案。
- 網站共有五種頁面：Home、Technology、Use Cases、About Us、Contact Us。
- `FACES` 收入 `About Us`，不獨立成頁。
- `Use Cases` 要徵求 POC 場域合作對象。
- 首頁須說明技術最終目標為降低碳排，並在有核實依據及 Company 書面核准後，說明對企業碳權／ESG 的可能價值。
- 技術名稱、效益數字、安全、法規、節能與碳權 wording 必須由 Company 確認；確認前不自行改寫或新增承諾。
- Email 內容本階段不修改，留待 Company 確認。
- CRM 工具選型要求已移除；只保留基本 CRM 功能範圍。
- 現場新增的學校或相關組織可納入 outreach範圍，不限原本三個單位。
- 費用包含最多三輪集中修改。
- 付款採分階段請款：
  - Website／CRM／academic outreach：USD 675
  - FUTEX 三日現場執行：USD 525
- 每張無爭議發票付款條件為 Net 30。
- 準據法為中華民國法律，並以臺灣臺北地方法院為第一審專屬管轄法院。
- 所有專案時程採臺灣時間（UTC+8）。
- 最新交付節點：
  - 第一版：2026-09-12
  - 第二版：2026-09-14
  - 最終版：2026-09-16

## Google 文件待修改項目

### 1. 交付日期

位置：`Statement of Work` → `3. Services and Deliverables` → `D. Website Optimization & CRM Integration` → `Schedule & Delivery`

將：

- `September 11, 2026` 改成 `September 12, 2026`
- `September 13, 2026` 改成 `September 14, 2026`
- `September 15, 2026` 改成 `September 16, 2026`

### 2. 一次性網站專案

位置：`Statement of Work` → `2. Team Roles and Responsibilities` → `Contractor` 列 → `Key Responsibilities`

將：

> permanent bilingual website optimization

改成：

> one-time bilingual website optimization

### 3. 開頭文法

位置：`Statement of Work` 標題下第一段。

- `This Statement of work` 改成 `This Statement of Work`
- `“Contractor” or “The Contractor).` 改成 `“Contractor” or “the Contractor”).`

### 4. Company 延遲造成時程順延

位置：`2. Team Roles and Responsibilities` → 表格下方 `Company Dependencies` 段落末尾。

新增：

> Any delay in required content, approvals, credentials, access, or third-party services will result in a corresponding extension of the delivery schedule unless both parties agree in writing to adjust the scope or priorities.

### 5. POC 執行條件

位置：`3.D Website Optimization & CRM Integration` → `Content Architecture & Value Proposition` → `Use Cases` 段落末尾。

新增：

> POC deployment is subject to site suitability, technical assessment, access arrangements, safety requirements, available resources, and a separate written agreement between the parties.

### 6. 個資管理細節

位置：`5. Additional Legal Terms` → `Confidentiality & Personal Data` 段落末尾。

新增：

> Website inquiry data will be accessible only to authorized personnel and used for responding to inquiries and managing follow-up activities. The Company is responsible for approving the data-retention period and internal access permissions before launch.

### 7. 終止時付款

位置：`5. Additional Legal Terms` → `Cancellation & Termination` 段落末尾。

新增：

> Upon any termination, the Company shall pay for all work completed up to the termination date and any approved, non-cancellable third-party costs already incurred.

### 8. 排除項目

位置：`3.D Website Optimization & CRM Integration` → `CRM Scope` 後方。

新增：

> Exclusions: Hosting fees, domain fees, paid plugins, third-party platform fees, marketing automation, quotation management, full sales pipeline functions, recruitment systems, and unverified technical or performance claims are excluded unless separately agreed in writing.

### 9. BACnet 拼法

位置：`3.D` → `Home & Technology`。

- 目前為 `BacNET-native`。
- `Website Guidelines` 使用 `BACnet-native`。
- 屬於技術 wording，先標記，等 Company 確認後再統一，不要自行修改。

## 已符合、不需修改

- `4. Review and Acceptance` 已寫明最多三輪集中修改。
- `6. Payment Schedule & Terms` 已包含 USD 675／USD 525 分階段請款。
- `Payment Terms` 已包含 Net 30、爭議項目須書面提出，以及無爭議款項的付款要求。
- `5. Additional Legal Terms` 已包含智慧財產權、保密與個資、取消與終止、責任限制。
- 管轄條款已採中華民國法律及臺灣臺北地方法院第一審專屬管轄。
- `Use Cases` 已包含 `Apply as a POC Site Partner`。
- `About Us` 已包含 FACES。
- `CRM Scope` 未要求指定 CRM 工具選型。
- Academic outreach 已允許活動現場新增相關組織。

## 本機文件狀態

以下檔案的交付日期已同步為 9/12、9/14、9/16：

- `ThermoVerse_網站內容規劃.md`
- `SOW_修改建議_2026-09-08.md`
- `SOW_Revision_Recommendations_EN.md`

網站內容與名詞的主要依據：

1. Google 文件的 `Website Guidelines`
2. `ThermoVerse_網站內容規劃.md`
3. `ThermoVerse_產品與名詞入門.md`

若資料互相衝突，以 Company 最新書面確認及 Google 文件 `Website Guidelines` 為準。

## 對 Company 說明修改內容的英文句子

- Updated the review and delivery dates to September 12, September 14, and September 16, 2026, based on Taiwan Time (UTC+8).
- Changed “permanent” to “one-time” to clarify the agreed website project scope.
- Corrected capitalization and quotation marks in the opening paragraph.
- Added a schedule-extension provision for delays in required content, approvals, credentials, access, or third-party services.
- Added conditions for POC deployment, including site suitability, technical assessment, access, safety requirements, available resources, and a separate written agreement.
- Added restrictions on access to website inquiry data and clarified the Company’s responsibility for approving data retention and access permissions.
- Added payment protection for completed work and approved, non-cancellable costs if the project is terminated.
- Added exclusions for hosting, domains, paid plugins, third-party fees, advanced sales and marketing functions, recruitment systems, and unverified claims.
- Flagged inconsistent BACnet capitalization for Company confirmation. No technical wording has been changed at this stage.

## 網站視覺與前端修改進度（2026-09-11 更新）

### 1. 全站字型與字距
- **字型**：全站統一引入 Google Fonts **Outfit**（weights: 300, 400, 500, 600, 700, 800），取代預設的系統 Inter 字型。
- **字距（Letter Spacing）**：全站統一調整為 **`0.09em`**（移除了原先 h1、h2 緊縮的負字距）。
- **最大容器寬度**：從 `1180px` 擴展至 **`1400px`**（`--max: 1400px`）。

### 2. Header 導覽列
- **樣式**：改為置頂固定透明漸層（`linear-gradient(180deg, rgba(9, 24, 35, 0.6) 0%, rgba(9, 24, 35, 0.25) 60%, transparent 100%)`），去除邊框，融入背景影片。
- **連結項目**：
  - 各頁導覽列**已移除「Home」**連結項目（目前為 `LATCHES`、`Use Cases & Services`、`About Us`、`Contact Us`）。
  - 點擊左側 ThermoVerse Logo 會返回首頁（`index.html`）。
  - `Technology (LATCHES)` 已全站簡化為 **`LATCHES`**。

### 3. 首頁 Hero 區塊（index.html）
- **背景影像**：改為全版滿版動態背景影片 `assets/hero.mp4`（100vh 撐滿、`object-fit: cover`、`autoplay muted loop playsinline`），備用 poster 為 `assets/hero-bg.png`。
- **文字內容與結構**：
  - **紅色標籤（Eyebrow）**：
    - 文案：`The world's first BACnet-native, zero-footprint solid-state thermal battery.`
    - 樣式：品牌紅 `#df4862`、字體大小 **`1.4rem`**（約 22.4px）、`font-weight: 600`、無文字陰影。
  - **主標題（h1）**：
    - 文案（雙行斷行）：
      ```
      Pioneering Thermal Storage for Today.
      Optimizing Building Energy Performance.
      ```
    - 樣式：字重改細為 **`font-weight: 350`**，尺寸為自適應 `clamp(2.1rem, 4.0vw, 4.0rem)`（桌面上限為 **4.0rem / 64px**，縮放比 **4.0vw**）。
    - 移除原本下方的描述長段落。
- **按鈕樣式與位置**：
  - 風格：**Liquid Glass（水感玻璃）**，高透光微光澤效果（`background: rgba(255,255,255,0.06)`、細邊框 `0.5px solid rgba(255,255,255,0.15)`、毛玻璃模糊與內陰影）。
  - 位置：下移緊貼 Hero 底部（`padding-bottom: 8px`、`margin-top: 44px`），讓上方留出充足視覺空間展現背景影片。
- **第二行標題字級微調**：
  - `Optimizing Building Energy Performance.` 字體大小依要求**縮小 10%**（桌面端由 `clamp(3rem, 5.04vw, 5.4rem)` 改為 `clamp(2.7rem, 4.54vw, 4.86rem)`；手機端由 `clamp(2.58rem, 12vw, 3.3rem)` 改為 `clamp(2.32rem, 10.8vw, 2.97rem)`）。其他版面架構與文字均完全維持不變。
- **紅色標籤字級微調**：
  - `The world's first BACnet-native, zero-footprint solid-state thermal battery.` 字體大小依要求**放大 25%**（桌面端由 `clamp(1rem, 1.35vw, 1.3rem)` 放大至 **`clamp(1.25rem, 1.69vw, 1.63rem)`**；手機端由 `0.94rem` 放大至 **`1.18rem`**）。
- **解除標題寬度瓶頸（防意外折行）**：
  - 將 `.animated-hero h1`、`.animated-hero .eyebrow`、`.animated-hero .hero-bottom h2` 的 `max-width` 全數改為 **`100%`**（移除原本鎖死在 950px / 1000px 的限制），在大螢幕或全螢幕視窗下拉到最寬時，各行文字能夠完整保持一整行自然舒展，不再被強制折行。

### 4. 挑戰區塊（#challenges）排版與卡片視覺升級（標題在上＋描述在下＋卡片加高）
- **標題與小標文案及順序**：
  - **紅色小標（.eyebrow）**：`The pressure facility teams face everyday`（置於大標題上方）
  - **大標題（h2）**：`Peak demand, comfort, and energy challenges`
- **字體樣式**：同步 Hero 細體規範（`font-weight: 350`、行高 `1.08`、寬度 `max-width: 100%`），小標與大標間維持適度呼吸間距（`margin-bottom: 14px`）。
- **移除數字標籤**：完全移除原先左上角的 `01`、`02`、`03`、`04` 浮雕圓章，卡片整體更洗鍊。
- **卡片加高（Taller Cards）**：
  - 卡片高度由 340px 顯著加高至 **`height: 440px; min-height: 440px;`**（手機端 `height: 360px`），呈現更大氣舒適的黃金長寬比例。
- **標題在上，描述在下（.challenge-inner）**：
  - 採用 `display: flex; flex-direction: column; justify-content: space-between;`：
    - **大標題（h3）**：置於卡片**最頂部**（`font-size: clamp(1.35rem, 1.75vw, 1.55rem)`，純白高對比與立體陰影）。
    - **中央畫面**：完全露出實景圖片核心主體，無任何文字遮擋。
    - **描述段落（.challenge-bottom p）**：固定置於卡片**最底部**（`font-size: 1.02rem`，白色高清晰度）。
- **四段式漸層遮罩（.challenge-overlay）**：
  - 疊加 `linear-gradient(180deg, rgba(9,24,35,0.76) 0%, rgba(9,24,35,0.22) 28%, rgba(9,24,35,0.38) 60%, rgba(9,24,35,0.88) 100%)`。
  - 頂部與底部加深保護上下文字的閱讀對比度，中央大面積半透光（22%~38%）完美顯露背景實景圖像。
- **互動效果**：
  - 滑鼠 Hover 時卡片平滑微浮（`-4px`）、底圖平滑放大（`scale(1.05)`）、遮罩微亮透光。

### 5. 核心方案卡片（#solutions）增高與專屬視覺圖更換
- **大標題修訂**：改為 `Two distinct pillars for building energy management.`。
- **上半部圖片高度增加 25%**：
  - `.solution-media` 高度由原先 `240px` 增加 25% 至 **`300px`**，卡片最小高度同步調整為 `min-height: 580px`，充分展現整棟智慧建築切面與天花板儲能電路網絡的科技細節。
- **更換為對應的高清實景 WebP 圖像**：
  - **ENERGY SERVICES（Act on today’s building needs.）**：替換為 `assets/Act on today’s building needs.webp`（現代多層智慧建築剖面、物聯網感測與 MEP 數位雙生儀表板）。
  - **LATCHES INNOVATION（Prepare assets for active thermal storage.）**：替換為 `assets/Prepare assets for active thermal storage.webp`（辦公空間上方 drop-ceiling 輕鋼架天花板與發光散熱管線/儲能電路網絡意象）。
  - 圖片具備滑鼠 hover 微放大互動效果（`scale(1.03)`）。
- **底部連結改為實心按鈕**：
  - 原先的紅色純文字連結（`Explore Energy Services` / `Explore LATCHES Technology`）改為大圓角立體膠囊按鈕（`.button`），並使用 `margin-top: auto` 齊底對齊，視覺更具點擊召喚力。

### 6. 應用場景卡片（#applications）排版與按鈕升級
- **卡片背景改為專屬高清大圖（位於 `assets/` 資料夾）**：
  - **Commercial buildings**：`assets/Commercial buildings.jpeg`（頂級玻璃雙子商辦大樓街景）。
  - **Industrial sites**：`assets/AI center.jpeg`（高科技浸沒式液冷 AI 運算中心與無塵技術人員現場）。
  - **Multifamily properties**：`assets/Multifamily properties.jpeg`（現代立體綠化景觀住宅大樓社區）。
- **移除重複的頂部紅字小標**：
  - 移除原先重複的 `<div class="num">COMMERCIAL</div>`、`INDUSTRIAL`、`MULTIFAMILY`，頂部僅保留大標題（`Commercial buildings`、`Industrial sites`、`Multifamily properties`），版面更俐落乾淨。
- **描述文字下移至按鈕上方**：
  - 描述段落（`<p>`）從原先卡片頂部移至底部容器（`.app-card-bottom`），緊貼於按鈕上方（`margin-bottom: 20px`），卡片中央露出清晰完整的背景照片主體。
- **按鈕改為精緻毛玻璃質感（Frosted Glass）**：
  - 由原先的紅色實心按鈕升級為 **Liquid Glass 毛玻璃膠囊按鈕**（`.button.glass`）。
  - 樣式：半透明高光白背景（`rgba(255, 255, 255, 0.1)`）、微光細邊框（`1px solid rgba(255, 255, 255, 0.22)`）、20px 背景高強度模糊（`backdrop-filter: blur(20px) saturate(1.5)`）及立體內外陰影，Hover 時發光強化（`background: rgba(255,255,255,0.2)`）。
- **漸層遮罩自適應優化**：
  - 頂部 0%（`0.6`）保護大標題閱讀性；中間 28%（`0.22`）高透光展現場景照片；底部 68%~100%（`0.72`~`0.95`）加深，完美烘托白色描述文字與毛玻璃按鈕。卡片高度設為 `min-height: 500px`。

### 7. 區塊順序微調：合作夥伴（#partners-awards）與 減碳效益（#measured-results）對調
- **順序對調**：
  - 第 5 區塊改為：**合作夥伴與獎項（`#partners-awards`）**，緊接在應用場景下方展示國際權威機構背書。
  - 第 6 區塊改為：**減碳效益與 ESG 報告（`#measured-results`）**，作為頁面收尾的權威決策依據。
  - 同步調整斑馬紋交錯背景樣式（`#partners-awards` 為純白 `section`，`#measured-results` 為淡漸層 `section alt`），維持整頁律動感。
- **合作夥伴內容（thermoverse.com 官網正式陣容）**：
  - 橫幅（`assets/partners-logos.png`）：Innovation Crossroads (ORNL)、U.S. Department of Energy (DOE)、RISE、IMPEL、NSF I-Corps。
  - 大標題：`Recognized by leading energy institutions and innovation programs.`。

### 8. 首頁 Hero 滿版防露白修復
- **修正右側露白問題**：
  - `.hero-content` 加入 `margin: 0 auto; box-sizing: border-box;`，避免在特定視窗寬度下內距（padding）疊加容器寬度導致橫向微溢出。
  - 在 `@media (max-width: 760px)` 響應式中，明確將 `.animated-hero` 排除在通用 `.hero` 的覆蓋規則之外（改為 `.hero:not(.animated-hero)`），確保在任何小視窗或筆電視窗縮放時，Hero 影片始終保持 100% 絕對全螢幕鋪滿，絕不露出下層白色背景。

### 9. 頁尾（.footer）高度依要求減少 20% 與底欄置中
- **頁尾高度減少 20%（黃金舒適比例）**：
  - `.footer` 上方內距由原基準 `84px` 減少 20% 至 **`68px`**（下方維持 `24px`）。
  - `.footer-brand p` 標語與 Logo 間距由 `28px` 減少 20% 至 **`22px`**。
  - `.footer-bottom` 與上方欄位的大間距由 `76px` 減少 20% 至 **`60px`**，分隔線內距縮為 `20px`。
  - 手機版頂部內距由 `64px` 減少 20% 至 **`52px`**，底欄間隔調整為 `45px`。
  - 整體高度相較初始版本減少精準的 20%，兼具呼吸感與現代緊湊度。
- **底欄置中排列**：
  - 原先採用 `justify-content: space-between`，改為**全寬度水平置中排列**（`justify-content: center; align-items: center; gap: 24px; text-align: center;`）。
  - 手機版同步維持置中對齊（`gap: 18px`），視覺整潔聚焦。

### 10. 頁尾 Logo 下方品牌標語更新（.footer-brand p）
- **文案調整**：
  - 原文案：`Comprehensive Energy Services today. BACnet-native, zero-footprint solid-state thermal storage for tomorrow.`
  - 新文案：**`Where Building Intelligence Meets Grid Resilience`**
  - 已全站同步更新至所有 6 個頁面（`index.html`、`about.html`、`contact.html`、`technology.html`、`use-cases.html`、`privacy.html`）。

### 11. 全站圖片資產 WebP 轉檔與格式全面升級
- **轉檔範圍與品質**：
  - 將 `website/assets/` 及其子目錄（`hero/`、`partners/`）下全部 21 張影像資產（`.png`、`.jpeg`、`.jpg`）全數轉檔為次世代 **`.webp`** 格式。
  - 照片類採用 Lossy 品質 88，含透明通道圖標類採用 Lossy 品質 90，兼顧最高視覺清晰度與最佳壓縮率。
  - 檔案體積大幅縮減（例如 `hero-bg.png` 由 5.6MB 縮減至 296KB，節省約 95%；`Commercial buildings.jpeg` 由 3.9MB 縮減至 1.0MB；全站資產總傳輸量大幅下降，頁面首屏載入速度顯著提升）。
- **程式碼引用全面更新**：
  - `styles.css`：更新三大應用場景背景大圖為 `.webp`。
  - `index.html`：更新 Header Logo、Hero Video Poster、4 張挑戰區塊大圖、2 張解決方案照片、合作夥伴機構圖標橫幅及 Footer Logo。
  - `about.html`：更新 Header Logo、工作坊照片（`workshop.webp`）、Footer Logo。
  - `technology.html`：更新 Header Logo、天花板安裝照片（`install.webp`）、Footer Logo。
  - `use-cases.html`：更新 Header Logo、Footer Logo。
  - `contact.html`：更新 Header Logo、Footer Logo。
  - `privacy.html`：更新 Header Logo、Footer Logo。
  - `assets/hero/scene.js`：更新開場城市天際線底圖（`city-opening.webp`）。
  - 所有次頁面的 CSS 快取版本號同步升級至 `styles.css?v=9`。
- **校驗確認**：
  - 全站 30 處 WebP 圖片引用路徑經自動化程式核對，全數精準對應磁碟檔案，0 個破圖與 0 個 404。
  - HTML/CSS/JS 內舊格式（`.png`, `.jpg`, `.jpeg`）引用已全部清零。

### 12. GitHub 倉庫初始化、帳號嚴格隔離與部署架構
- **遠端倉庫位址**：
  - `https://github.com/thermo2026/thermoverse-website.git`
- **獨立 Git 身分與帳號隔離（重要）**：
  - 本專案嚴格獨立，與使用者的個人專案 `n239-studio` **完全隔離**。
  - 本地倉庫獨立設定：
    - `user.name = thermo2026`
    - `user.email = thermo2026@users.noreply.github.com`
  - 歷史所有 Commit 的作者（Author & Committer）均已透過 rebase 完成清洗，無任何私人身分殘留。
  - 遠端推送採用 `thermo2026` 專屬 Personal Access Token（PAT），不影響系統全域 SSH 金鑰與其他專案。
- **倉庫檔案與過濾機制**：
  - 根目錄新增標準 [`.gitignore`](.gitignore)，有效過濾 `tmp/`（Pillow 二進位庫等）、`.build/`（投影片產出檔）、`output/`（影片截圖）及 `.DS_Store`，節省超過 200MB 的上傳空間。
  - 根目錄新增正式專案說明文件 [`README.md`](README.md)，記載本地啟動（Python HTTP Server）與部署說明。
- **分支雙軌架構**：
  - **`main` 分支**：存放完整專案程式碼、SOW 法律與規劃文件、設計文件，並在根目錄提供自動跳轉至 `website/` 的 `index.html`。
  - **`gh-pages` 分支**：透過 `git subtree split` 自動生成，直接將 `website/` 目錄提升至根層級，專門供 GitHub Pages 靜態伺服器部署。
- **GitHub Pages 免費上線指引**：
  - 由於免費版 GitHub Pages 僅支援 **Public** 倉庫：
    1. 前往 GitHub 倉庫 **Settings** → **Danger Zone** 將可見度改為 **Public**。
    2. 前往 **Settings** → **Pages** → **Build and deployment** 下方的 Branch 選擇 **`gh-pages`** / **`/(root)`** 並儲存。
    3. 即可獲得官方線上預覽網址：`https://thermo2026.github.io/thermoverse-website/`。

### 13. Technology (LATCHES) 頁面 Hero 標題與版面優化
- **標題調整與描述段落移除（technology.html）**：
  - 原標題：`Turn Your Ceiling Into a Programmable Thermal Battery.`
  - 新標題：**`The world's most compact, zero-footprint thermal energy storage solution`**
  - **移除描述長段落**：依需求拿掉原先下方長篇說明段落，使 Hero 區塊視覺更洗鍊聚焦、直接引導至下方核心方案。

### 14. 全站字體大小與排版階層全面比照首頁標準化
- **Hero 大標題（.page-hero h1）比照首頁 Hero**：
  - 由原先過大的 `clamp(3rem, 6.3vw, 5.9rem)`、重字重 `550`，全面比照首頁改為：
    - `font-size: clamp(2.5rem, 4.2vw, 4.5rem)`
    - `font-weight: 350`（優雅輕盈字重）
    - `line-height: 1.04`、`letter-spacing: -0.025em`
    - 最大寬度放寬至 `960px`，使長標題換行更平順
    - 手機版同步改為 `clamp(2.15rem, 10vw, 2.75rem)`，與首頁完全一致。
- **Hero 說明內文（.page-hero p）比照首頁段落**：
  - 調整為 `font-size: 1.08rem; color: var(--muted);`，與首頁各區塊引言（`.section-intro`）完全同規。
- **卡片與條列標題（.card h3, .step h3, .line-list h3）比照首頁卡片**：
  - `.card h3` 由原 `1.25rem` 統一調整為 **`1.45rem; font-weight: 600;`**，與首頁解決方案與應用卡片標題完全對齊。
  - `.step h3` 與 `.line-list h3` 統一明確規範為 `1.25rem; font-weight: 600;`。
- **全站 CSS 版本升級**：
  - 所有頁面升級至 `styles.css?v=16`，即時突破瀏覽器快取。

### 15. 全站次頁 Hero 高度全面比照首頁 100vh 滿版對齊
- **高度統一為滿版視窗高度（.page-hero）**：
  - 設定 `min-height: 100vh; min-height: 100svh;`，與首頁 Hero（`.animated-hero`）完全一致。
  - 採用 Flexbox 垂直置中佈局（`display: flex; flex-direction: column; justify-content: center;`），搭配彈性安全內距（`padding: clamp(112px, 14vh, 160px) 0 clamp(40px, 8vh, 80px);`），完美避開頂部固定式導覽列。
  - 手機版同步維持 `min-height: 100vh; min-height: 100svh;`。
- **全站 CSS 快取版本升級至 v=17**：
  - 所有 6 個頁面已同步更新為 `styles.css?v=17`。

### 16. Technology (LATCHES) 頁面各區塊小標數字移除
- **移除小標編號（.eyebrow）**：
  - `01 · LATCHES thermal battery overview` ➔ **`LATCHES thermal battery overview`**
  - `02 · Phase Change Materials` ➔ **`Phase Change Materials`**
  - `03 · Sensing, storage, and control` ➔ **`Sensing, storage, and control`**
  - `04 · Ceilings and walls` ➔ **`Ceilings and walls`**
  - `05 · Heat-flow control and peak management` ➔ **`Heat-flow control and peak management`**
  - `06 · FAQ` ➔ **`FAQ`**
  - 全數移除前綴編號與中圓點，版面與首頁、Use Cases 頁面的乾淨無編號風格保持一致。

### 17. Technology (LATCHES) 頁面 Hero 按鈕移除並換為描述文字
- **Hero 結構微調（technology.html）**：
  - 移除原 Hero 下方的按鈕（`<a class="button">Discuss Your Requirements</a>`）。
  - 替換為全新精簡描述段落：
    ```html
    <p>Solid-state thermal battery designed for dense urban buildings</p>
    ```
  - 樣式套用 `.page-hero p`（`font-size: 1.08rem; color: var(--muted);`），與首頁各區塊排版規格完全同軌，畫面更具品牌高級感。

### 18. Technology (LATCHES) 頁面移除重複開頭文案
- **移除冗餘語句（#thermal-battery 區塊）**：
  - 原文案：`LATCHES is a zero-footprint, solid-state thermal battery designed for dense urban buildings. By integrating thermal storage into existing ceiling or wall areas...`
  - 調整後：**`By integrating thermal storage into existing ceiling or wall areas, it offers an alternative where traditional chemical Battery Energy Storage Systems (BESS) may be restricted by urban fire codes or space constraints.`**
  - 效益：因上方 Hero 已包含專屬描述 `Solid-state thermal battery designed for dense urban buildings`，移除此句後消除上下重複，內容更加精練緊湊。

### 19. Technology (LATCHES) 頁面小標優化
- **小標更新（#thermal-battery 區塊）**：
  - 原小標：`LATCHES thermal battery overview`
  - 新小標：**`Urban-safe energy storage`**
  - 效益：更精確聚焦於其解決都會消防法規（Fire Codes）與安全替代化學電池的核心定位。

### 20. Technology (LATCHES) 頁面區塊順序互換（Sensing & Control ↔ Phase Change Materials）
- **區塊順序調整（technology.html）**：
  - 第 2 區塊改為：**`Sensing, storage, and control`**（`#sensing-control`），先闡述感知、預測與充放電控制流程（步驟 1 Sense / 2 Predict / 3 Charge or discharge）。
  - 第 3 區塊改為：**`Phase Change Materials`**（`#pcm-storage`），接著深入介紹 102 BTU/SF 相變蓄熱材料特性與儲能卡片。
  - **背景樣式自適應調適**：同步調整斑馬紋交錯背景樣式（`#sensing-control` 設為 `section alt` 淡漸層，`#pcm-storage` 設為純白 `section`），維持全頁視覺律動節奏。

### 21. Technology (LATCHES) 頁面移除內部準則對比句
- **移除安裝說明冗餘內部句（#installation 區塊）**：
  - 原文案：`LATCHES is designed for drop-ceiling and wall integration, preserving rentable floor area while placing thermal storage close to the conditioned space. Its installation approach is designed to be eight times faster than the comparison system referenced in the Company’s Website Guidelines.`
  - 調整後：**`LATCHES is designed for drop-ceiling and wall integration, preserving rentable floor area while placing thermal storage close to the conditioned space.`**
  - 效益：移除帶有內部文件條款風味的比較指引句，使面向客戶的行銷與技術文案更加純粹自然。










